# Production Environment Variables
# Скопируйте в .env и измените значения!

# PostgreSQL Configuration (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
POSTGRES_PASSWORD=YOUR_SUPER_SECURE_DATABASE_PASSWORD_HERE

# JWT Configuration (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ! Минимум 32 символа)
JWT_SECRET_KEY=YOUR_SUPER_SECURE_JWT_SECRET_KEY_MINIMUM_32_CHARACTERS_LONG

# Email Configuration (опционально, для отправки писем)
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password
EMAIL_USE_SSL=true
EMAIL_FROM=your-email@gmail.com

# Генерация безопасных паролей:
# Linux/macOS/Git Bash:
#   openssl rand -base64 32
#
# PowerShell:
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
#
# Online:
#   https://www.random.org/strings/
