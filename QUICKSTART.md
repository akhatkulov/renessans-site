# ⚡ Быстрый старт Renessans Site

## 🐳 Docker (рекомендуется - 2 минуты)

Если у вас установлен Docker:

**Windows:**
```bash
docker-start.bat
```

**Linux/macOS:**
```bash
chmod +x docker-start.sh
./docker-start.sh
```

Откройте: http://localhost:5000/swagger

📖 Подробнее: [DOCKER_QUICKSTART.md](DOCKER_QUICKSTART.md)

---

## 💻 Локальная установка (5 минут)

Если хотите запустить без Docker:

## Минимальные шаги для запуска (5 минут)

### 1️⃣ Установите необходимое ПО

- [ ] **.NET 9.0 SDK** → https://dotnet.microsoft.com/download/dotnet/9.0
- [ ] **PostgreSQL** → https://www.postgresql.org/download/

### 2️⃣ Создайте базу данных

```bash
# Войдите в psql
psql -U postgres

# Создайте БД
CREATE DATABASE renessans;

# Выйдите
\q
```

### 3️⃣ Настройте проект

Отредактируйте `backend/RenessansAPI/appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "host=localhost; port=5432; username=postgres; password=ВАШ_ПАРОЛЬ; Database=renessans;"
  },
  "JWT": {
    "Key": "любая-случайная-строка-минимум-32-символа-abcdef123456",
    ...
  }
}
```

### 4️⃣ Запустите проект

**Windows:**
```bash
start.bat
```

**macOS/Linux:**
```bash
chmod +x start.sh
./start.sh
```

**Вручную:**
```bash
cd backend/RenessansAPI
dotnet restore
dotnet run
```

### 5️⃣ Откройте в браузере

🎉 **Готово!** Откройте: https://localhost:5001/swagger

---

## 🔍 Проверка перед запуском

Запустите скрипт проверки:

**Windows:** `setup-check.bat`  
**Linux/Mac:** `./setup-check.sh`

---

## 📚 Полная документация

- **Детальная установка**: [SETUP.md](SETUP.md)
- **Backend документация**: [backend/README.md](backend/README.md)
- **Общая информация**: [README.md](README.md)

---

## 🆘 Не работает?

### PostgreSQL не подключается

```bash
# Проверьте, что PostgreSQL запущен
# Windows: Откройте "Службы" → найдите postgresql
# Linux/Mac:
pg_isready
```

### Порт занят

Измените порт в `backend/RenessansAPI/Properties/launchSettings.json`

### Другие проблемы

См. раздел "Решение проблем" в [SETUP.md](SETUP.md)

---

## 🎯 Что дальше?

1. Протестируйте API через Swagger UI
2. Изучите доступные endpoints
3. Посмотрите автоматически созданные роли и пользователей
4. Начните разработку Frontend

---

**Вопросы?** Проверьте полную документацию или логи в консоли.
