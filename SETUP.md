# 🚀 Руководство по установке и настройке Renessans Site

## Пошаговая инструкция для начинающих

### Шаг 1: Установка необходимого ПО

#### 1.1 Установка .NET 9.0 SDK

1. Перейдите на сайт: https://dotnet.microsoft.com/download/dotnet/9.0
2. Скачайте установщик для вашей операционной системы:
   - **Windows**: x64 Installer
   - **macOS**: x64 или ARM64 (для M1/M2)
   - **Linux**: Следуйте инструкциям для вашего дистрибутива
3. Запустите установщик и следуйте инструкциям
4. Проверьте установку, открыв терминал и выполнив:
   ```bash
   dotnet --version
   ```
   Должна отобразиться версия 9.x.x

#### 1.2 Установка PostgreSQL

1. Перейдите на сайт: https://www.postgresql.org/download/
2. Выберите версию для вашей ОС (рекомендуется PostgreSQL 15 или 16)
3. Запустите установщик
4. **ВАЖНО**: Запомните пароль для пользователя `postgres`
5. Порт по умолчанию: `5432` (оставьте как есть)
6. После установки проверьте, что PostgreSQL запущен:
   
   **Windows**: Откройте "Службы" (services.msc) и найдите postgresql
   
   **macOS/Linux**:
   ```bash
   pg_isready
   ```

#### 1.3 (Опционально) Установка IDE

Выберите один из вариантов:

- **Visual Studio 2022 Community** (Windows) - https://visualstudio.microsoft.com/
- **JetBrains Rider** - https://www.jetbrains.com/rider/
- **Visual Studio Code** - https://code.visualstudio.com/ + C# Dev Kit расширение

### Шаг 2: Настройка базы данных

#### 2.1 Создание базы данных

**Вариант А: Через pgAdmin (графический интерфейс)**

1. Откройте pgAdmin (устанавливается вместе с PostgreSQL)
2. Подключитесь к серверу (введите пароль postgres)
3. Правый клик на "Databases" → "Create" → "Database"
4. Введите имя: `renessans`
5. Нажмите "Save"

**Вариант Б: Через командную строку**

```bash
# Войдите в psql
psql -U postgres

# Создайте базу данных
CREATE DATABASE renessans;

# Проверьте создание
\l

# Выйдите
\q
```

#### 2.2 (Опционально) Создание отдельного пользователя

```sql
-- В psql выполните:
CREATE USER renessans_user WITH PASSWORD 'ваш_безопасный_пароль';
GRANT ALL PRIVILEGES ON DATABASE renessans TO renessans_user;
```

### Шаг 3: Клонирование и настройка проекта

#### 3.1 Клонирование репозитория

```bash
git clone <URL_репозитория>
cd renessans-site
```

#### 3.2 Настройка конфигурации

1. Откройте файл `backend/RenessansAPI/appsettings.json`

2. Найдите секцию `ConnectionStrings` и замените параметры:

```json
"ConnectionStrings": {
  "DefaultConnection": "host=localhost; port=5432; username=postgres; password=ВАШ_ПАРОЛЬ_POSTGRES; Database=renessans;"
}
```

3. Найдите секцию `JWT` и замените ключ на любую случайную строку (минимум 32 символа):

```json
"JWT": {
  "Key": "ваш-супер-секретный-ключ-минимум-32-символа-длиной-abc123xyz",
  "ValidIssuer": "RenessansAPI",
  "Expire": "2",
  "ResExpire": "30",
  "Audience": "Renessans.uz"
}
```

💡 **Совет**: Для генерации безопасного ключа можно использовать онлайн генератор или команду:
```bash
# PowerShell
[System.Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes([guid]::NewGuid().ToString() + [guid]::NewGuid().ToString()))
```

### Шаг 4: Проверка готовности

Запустите скрипт проверки:

**Windows**:
```bash
setup-check.bat
```

**macOS/Linux**:
```bash
chmod +x setup-check.sh
./setup-check.sh
```

Скрипт проверит:
- ✅ Установлен ли .NET SDK
- ✅ Установлен ли PostgreSQL
- ✅ Правильная ли структура проекта
- ⚠️ Обновлены ли конфигурационные файлы

### Шаг 5: Запуск проекта

#### Способ 1: Автоматический запуск (рекомендуется)

**Windows**:
```bash
start.bat
```

**macOS/Linux**:
```bash
chmod +x start.sh
./start.sh
```

#### Способ 2: Ручной запуск

```bash
cd backend/RenessansAPI
dotnet restore
dotnet run
```

#### Способ 3: Через IDE

1. Откройте `backend/Renessans.sln` в Visual Studio
2. Нажмите F5 или кнопку "Start Debugging"

### Шаг 6: Проверка работы

После запуска откройте в браузере:

1. **Swagger UI**: https://localhost:5001/swagger
   - Здесь можно протестировать все API endpoints

2. **Health Check**: https://localhost:5001/api/enum/gettypes
   - Должен вернуть список enum типов

### Шаг 7: Первый вход

При первом запуске автоматически создаются:
- Администратор по умолчанию
- Роли и разрешения

Чтобы узнать учетные данные администратора:
1. Проверьте файл `backend/RenessansAPI.Service/Seeders/DbSeeder.cs`
2. Или проверьте логи в консоли при первом запуске

---

## 🔧 Решение типичных проблем

### Проблема 1: "dotnet: command not found"

**Решение**: .NET SDK не установлен или не добавлен в PATH
- Переустановите .NET SDK
- Перезапустите терминал
- Windows: Перезагрузите компьютер

### Проблема 2: "Connection refused" к PostgreSQL

**Решение**:
1. Убедитесь, что PostgreSQL запущен
2. Проверьте порт (по умолчанию 5432)
3. Проверьте пароль в appsettings.json
4. Попробуйте подключиться через psql:
   ```bash
   psql -U postgres -d renessans
   ```

### Проблема 3: "Unable to create migration"

**Решение**:
```bash
cd backend
dotnet tool install --global dotnet-ef
dotnet ef database update --project RenessansAPI.DataAccess --startup-project RenessansAPI
```

### Проблема 4: Порт 5001 уже занят

**Решение**: Измените порт в `backend/RenessansAPI/Properties/launchSettings.json`:
```json
"applicationUrl": "https://localhost:5002;http://localhost:5003"
```

### Проблема 5: Ошибки миграции при первом запуске

**Решение**: Миграции применяются автоматически. Если возникают ошибки:
```bash
cd backend/RenessansAPI
dotnet ef database drop --force
dotnet ef database update
```

---

## 📝 Дополнительная настройка

### Настройка Email (опционально)

Если нужна отправка email, обновите секцию `Email` в appsettings.json:

```json
"Email": {
  "SmtpHost": "smtp.gmail.com",
  "SmtpPort": 587,
  "UserName": "your-email@gmail.com",
  "Password": "your-app-password",
  "UseSsl": true,
  "From": "your-email@gmail.com",
  "DisplayName": "Renessans"
}
```

💡 Для Gmail используйте App Password: https://myaccount.google.com/apppasswords

### Использование User Secrets (безопасность)

Вместо хранения паролей в appsettings.json используйте User Secrets:

```bash
cd backend/RenessansAPI
dotnet user-secrets init
dotnet user-secrets set "ConnectionStrings:DefaultConnection" "host=localhost;port=5432;username=postgres;password=your_password;Database=renessans;"
dotnet user-secrets set "JWT:Key" "your-secret-key"
```

---

## ✅ Чек-лист готовности

Перед запуском убедитесь:

- [ ] .NET 9.0 SDK установлен (`dotnet --version`)
- [ ] PostgreSQL установлен и запущен
- [ ] База данных `renessans` создана
- [ ] `appsettings.json` обновлен (пароль БД и JWT ключ)
- [ ] Зависимости восстановлены (`dotnet restore`)
- [ ] Порты 5000/5001 свободны
- [ ] Директории для изображений созданы

---

## 📞 Получить помощь

Если что-то не работает:

1. Проверьте логи в консоли - там обычно указана причина ошибки
2. Убедитесь, что все шаги выполнены по порядку
3. Проверьте, что PostgreSQL действительно запущен
4. Попробуйте запустить `setup-check.bat` для диагностики

---

**Готово!** 🎉 Теперь вы можете начать работу с API через Swagger UI.
