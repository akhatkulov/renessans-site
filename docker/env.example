# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=renessans
DB_USER=postgres
DB_PASSWORD=postgres

# JWT Configuration
JWT_KEY=change-this-to-a-secure-secret-key-minimum-32-characters-long
JWT_ISSUER=RenessansAPI
JWT_AUDIENCE=Renessans.uz
JWT_EXPIRE_HOURS=2
JWT_REFRESH_EXPIRE_DAYS=30

# Email Configuration (Optional)
EMAIL_SMTP_HOST=smtp.yourprovider.uz
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=no-reply@yourdomain.uz
EMAIL_PASSWORD=your-smtp-password
EMAIL_USE_SSL=true
EMAIL_FROM=no-reply@yourdomain.uz
EMAIL_DISPLAY_NAME=Renessans

# Application Settings
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001

# Pagination
DEFAULT_PAGE_SIZE=10
MAX_PAGE_SIZE=50
